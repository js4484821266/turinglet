{
  "name": "turinglet",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend",
    "shared",
    "database",
    "scheduler"
  ],
  "scripts": {
    "predev": "npm run build -w shared && npm run build -w scheduler && npm run build -w database && npm run migrate",
    "dev": "concurrently --kill-others --kill-others-on-fail \"npm run dev -w backend\" \"npm run dev -w frontend\"",
    "dev:server": "npm run dev -w backend",
    "dev:client": "npm run dev -w frontend",
    "dev:desktop": "concurrently --kill-others --kill-others-on-fail \"npm run dev -w backend\" \"npm run dev -w frontend\" \"npm run dev:electron\"",
    "dev:electron": "cross-env ELECTRON_START_URL=http://localhost:5173 electron frontend/electron/main.mjs",
    "build": "npm run build -w shared && npm run build -w scheduler && npm run build -w database && npm run build -w backend && npm run build -w frontend",
    "lint": "npm run lint -ws",
    "test": "npm run test -w backend",
    "migrate": "npm run migrate -w database",
    "seed": "npm run seed -w database",
    "llm:server": "cross-env KMP_DUPLICATE_LIB_OK=TRUE .\\.venv-llm\\Scripts\\python.exe local-llm/server.py"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8.22.0",
    "@typescript-eslint/parser": "^8.22.0",
    "concurrently": "^9.1.0",
    "cross-env": "^7.0.3",
    "electron": "^33.2.1",
    "eslint": "^8.57.1",
    "eslint-config-prettier": "^9.1.0",
    "eslint-plugin-import": "^2.31.0",
    "prettier": "^3.4.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  }
}
